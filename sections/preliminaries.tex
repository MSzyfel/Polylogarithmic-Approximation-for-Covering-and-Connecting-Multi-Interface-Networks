\section{Preliminaries}

Let $G=\br{V, E}$ be a connected simple graph. Let $[k]$ be the set of different types of interfaces across the network and let $\lambda\colon V \to [k]$ be a function that assigns to each vertex $v\in V$ its set of available interfaces such that for each edge $uv \in E$, $\lambda\br{u} \cap \lambda\br{v} \neq \emptyset$.
An assignment of active interfaces is a function $\lambda_A\colon V \to [k]$ such that $\lambda_A\br{v} \subseteq \lambda\br{v}$ for all $v\in V$. We say that an edge $uv \in E$ is \emph{covered} if $\lambda_A\br{u} \cap \lambda_A\br{v} \neq \emptyset$ and we denote by $G_A$ the subgraph of $G$ induced by covered edges.

In the \emph{coverage problem}, we wish to find an assignment of active interfaces $\lambda_A$ such that $G_A=G$. We call such an assignment a \emph{covering assignment}.

In the \emph{connectivity problem}, we are additionally given a set of \emph{terminals} $U\subseteq V$ and we wish to find an assignment of active interfaces $\lambda_A$ such that all vertices in $U$ lie in the same connected component of $G_A$. Vertices in $V\setminus U$ may be used as Steiner vertices. We call such an assignment a \emph{connecting assignment}. The special case $U=V$ corresponds to requiring that $G_A$ is a connected subgraph of $G$ spanning $V$.

We are also given a cost function $c\colon [k]\times V \to \mathbb{N}_{\geq 0}$, which denotes the cost of activating the interface $i\in[k]$ in the vertex $v\in V$.
The sum cost of an assignment $\lambda_A$ is defined as:
$$\COST_S=\sum_{v\in V}\sum_{i\in\lambda_A\br{v}}c\br{i, v}$$
and the max cost of and assignment $\lambda_A$ is defined as:
$$\COST_M=\max_{v\in V}\sum_{i\in\lambda_A\br{v}}c\br{i, v}.$$
The goal of the min-sum (resp. min-max) assignment problem is to find an assignment $\lambda_A$ that minimizes $\COST_S$ (resp. $\COST_M$).
%When not mentioned otherwise, we will focus on the min-max cost criterion.
The considered problems are formalized below:

\pbdef{\textsc{Coverage Problem}}{A connected simple graph $G=(V,E)$, available interfaces $\lambda\colon V \to [k]$, a cost function $c\colon [k]\times V \to \mathbb{N}_{\geq 0}$.}{Find a min-sum (resp. min-max) covering assignment $\lambda_A$ minimizing $\COST_S$ (resp. $\COST_M$).}

\pbdef{\textsc{Connectivity Problem}}{A connected simple graph $G=(V,E)$, available interfaces $\lambda\colon V \to [k]$, a cost function $c\colon [k]\times V \to \mathbb{N}_{\geq 0}$, set of terminals $U \subseteq V$.}{Find a min-sum (resp. min-max) connecting assignment $\lambda_A$ minimizing $\COST_S$ (resp. $\COST_M$).}

Additionally, we need the following probability inequality known as the Chernoff bound \cite{AMeasureOfAsymptoticEfficiencyForTestsOfAHypothesisBasedOnTheSumOfObservations}:
\begin{theorem}[Chernoff Bound]
Let $X_1, \dots, X_n$ be independent random variables taking values in $\{0, 1\}$, $w_1, \dots, w_n \in [0, 1]$ be weights, and let $X = \sum_{i=1}^n w_i X_i$. Then for every $\delta > 0$:
\[
\Pr[X \geq (1+\delta)\cdot\mathbb{E}[X]] \leq \left(\frac{e^\delta}{(1+\delta)^{1+\delta}}\right)^{\mathbb{E}[X]}
\]
and for every $0<\delta<1$:
\[
\Pr[X \leq (1-\delta)\cdot\mathbb{E}[X]] \leq \left(\frac{e^{-\delta}}{(1-\delta)^{1-\delta}}\right)^{\mathbb{E}[X]}.
\]
\end{theorem}

